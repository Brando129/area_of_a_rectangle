# Create a function that is able to tell a user what the area of a rectangle is

def find_area(length, width):
    length = length
    width = width
    area = length * width

    return f"The area of your rectangle is {area}"

print(find_area(20, 8))